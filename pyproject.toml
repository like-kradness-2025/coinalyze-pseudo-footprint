[project]
name = "coinalyze-pseudo-footprint"
version = "0.1.0"
description = "OHLCV-based pseudo footprint renderer for Coinalyze Receiver data."
requires-python = ">=3.10"
dependencies = ["pandas>=2.0", "numpy>=1.23", "matplotlib>=3.7"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
